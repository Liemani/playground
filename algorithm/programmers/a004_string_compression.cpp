#include <string>
#include <vector>

int solution(std::string s) {
  int answer = 0;

  for (int i = 0; i < s.length / 2; ++i) {
    const char* compressUnitStart = s.c_str();
    int numberOfRepetition = 1;
    for (eye until end cycle) {
    }

  }

  return answer;
}

int main() {
  printf(solution("abab"));

  return 0;
}

#include <stdio.h>
#include <stdlib.h>
#include "t_queue.h"
#include "t_search_list.h"

void	search(t_search_list p_parent)
{
	t_search_list	p_element;

 	p_element = new_element(p_parent, p_array_a, p_array_b);
}

t_search_list	*new_element(t_search_list *p_parent)
{
	t_search_list *const	p_element = lmt_alloc(sizeof(t_search_list));
	int						index;

	if (p_parent->rear < front)
		0 ~ rear, front ~ count
	else
	{
		index = 
		while (p_int != p_parent->p_rear_a)
	}


	return (p_element);
}

int	main(int argc, char **argv)
{
	const t_instruction	instruction_array[INSTRUCTION_COUNT] = {pa, pb, sa, sb, ss, ra, rb, rr, rra, rrb, rrr};
	int					instruction_index;
	t_search_list		p_list;

	instruction_index = 0;
	while (count < maximum count)
	{
		if (check(instruction))
		{
			make child;
			do instruction;
		}
	}
	return (0);
}
